<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p>There are <%= @releases.size %> new releases waiting for you!</p>
    <p>
      <% @releases.each do |release| %>
        <p>
          <a href="<%= release.uri %>">
            <% if release.thumb.presence %>
              <img style="height: 75px; width: 75px;" src="<%= release.thumb %>"/>
              <br/>
            <% end %>
            <strong><%= release.title %></strong> by <%= release.artist.name %>
          </a>
        </p>
      <% end %>
    </p>
  </body>
</html>